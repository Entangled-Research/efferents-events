# Falsifiability record

The seed hypothesis is bounded to a deterministic synthetic simulator, names a
numeric effect size, requires paired scenarios, fixes a 12-seed minimum, and
has a completion-rate safety constraint. Any change to the claim must create a
new Popper-corpus entry; owner steering may redirect future work but never edit
recorded evidence.
